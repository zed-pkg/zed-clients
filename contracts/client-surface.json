{
  "$schema": "./client-surface.schema.json",
  "contractVersion": "1.0.0",
  "product": "zed",
  "provenance": {
    "source": "clients/api-surface.json",
    "sourceKind": "api-surface",
    "note": "Reviewed initial projection matching `ores-clients-core import-api-surface`; the richer clients/api-surface.json remains the migration source until this projection is promoted."
  },
  "operations": [
    {
      "name": "create_zed_client",
      "doc": "Construct the standard client entry point.",
      "async": false,
      "params": [
        {
          "name": "options",
          "type": "object",
          "required": true
        }
      ],
      "returns": {
        "type": "object"
      }
    },
    {
      "name": "health",
      "doc": "Public client health operation imported from ZedClient.health.",
      "async": true,
      "params": [],
      "returns": {
        "type": "boolean"
      }
    },
    {
      "name": "send",
      "doc": "Public transport send operation imported from ZedTransport.send.",
      "async": true,
      "params": [
        {
          "name": "path",
          "type": "string",
          "required": true
        },
        {
          "name": "body",
          "type": "bytes",
          "required": false
        }
      ],
      "returns": {
        "type": "bytes"
      }
    }
  ]
}

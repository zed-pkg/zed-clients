import { createHash } from 'node:crypto';
export const CONTRACT_IR_SCHEMA='ores.typespec-json-schema-validator.contract-ir/v1';
export const PARITY_REPORT_SCHEMA='ores.typespec-json-schema-validator.report/v1';
const HEX=/^[a-f0-9]{64}$/u; const SETS=new Set(['allOf','anyOf','enum','oneOf','required','type']);
const obj=(v)=>v!==null&&typeof v==='object'&&!Array.isArray(v);
function need(c,m){if(!c)throw new Error(`contract-ir admission rejected: ${m}`);}
function canon(v,k=''){if(Array.isArray(v)){const xs=v.map((x)=>canon(x));if(!SETS.has(k))return xs;const m=new Map(xs.map((x)=>[JSON.stringify(x),x]));return [...m.entries()].sort(([a],[b])=>a.localeCompare(b)).map(([,x])=>x);}if(!obj(v))return v;const r={};for(const key of Object.keys(v).sort())r[key]=canon(v[key],key);return r;}
export const canonicalStringify=(v)=>JSON.stringify(canon(v));
export const sha256Json=(v)=>createHash('sha256').update(canonicalStringify(v)).digest('hex');
function digest(v,l){need(typeof v==='string'&&HEX.test(v),`${l} must be a lowercase SHA-256 digest`);return v;}
export function verifyContractIrAdmission({contractIr,parityReport,expectedInputs,requireComplete=false}){need(obj(contractIr),'contractIr must be an object');need(obj(parityReport),'parityReport must be an object');need(obj(expectedInputs),'expectedInputs is required');need(contractIr.schema===CONTRACT_IR_SCHEMA,'unexpected Contract IR schema');need(contractIr.status==='passed','Contract IR status must be passed');need(contractIr.admissible===true,'Contract IR must be admissible');need(contractIr.role==='downstream-derived-parity-artifact','Contract IR role is invalid');need(contractIr.editableAuthority===false,'Contract IR must not be an editable authority');need(contractIr.authorities?.typespec==='independently-authored','TypeSpec must remain independently authored');need(contractIr.authorities?.jsonSchema==='independently-authored','JSON Schema must remain independently authored');need(contractIr.authorities?.generatedJsonSchema==='comparison-evidence-only','generated JSON Schema must remain comparison evidence only');need(contractIr.authorities?.precedence==='none','peer authorities must have no precedence');need(parityReport.schema===PARITY_REPORT_SCHEMA,'unexpected parity report schema');need(parityReport.status==='passed','parity report status must be passed');need(parityReport.zeroUnexplainedFindings===true,'parity report must have zero unexplained findings');need(Array.isArray(parityReport.findings)&&parityReport.findings.length===0,'parity report findings must be empty');digest(parityReport.runId,'parityReport.runId');const receipt=contractIr.admission?.receipt;need(obj(receipt),'Contract IR receipt is missing');need(receipt.schema===parityReport.schema,'receipt schema does not match parity report');need(receipt.runId===parityReport.runId,'receipt runId does not match parity report');need(receipt.status==='passed','receipt status must be passed');need(receipt.zeroUnexplainedFindings===true,'receipt must bind zero unexplained findings');need(receipt.digest===sha256Json(parityReport),'receipt digest does not match the supplied parity report');const id=digest(contractIr.irId,'contractIr.irId');const body={...contractIr};delete body.irId;need(id===sha256Json(body),'Contract IR self digest does not match its body');for(const [rk,pk,ek] of [['typespec','typespec','typespec'],['authoredJsonSchema','authoredJsonSchema','authoredJsonSchema'],['generatedJsonSchema','generatedJsonSchema','generatedJsonSchema']]){const e=digest(expectedInputs[ek],`expectedInputs.${ek}`);const r=digest(parityReport.inputs?.[rk]?.digest,`parityReport.inputs.${rk}.digest`);const p=digest(contractIr.provenance?.[pk]?.digest,`contractIr.provenance.${pk}.digest`);need(r===e,`${rk} parity-report digest is stale for this checkout`);need(p===e,`${pk} Contract IR digest is stale for this checkout`);}if(requireComplete)need(contractIr.admission?.scope?.complete===true,'consumer requires a complete Contract IR scope');need(Array.isArray(contractIr.declarations),'Contract IR declarations are missing');for(const d of contractIr.declarations){need(obj(d),'Contract IR declaration must be an object');digest(d.assertionDigest,`declaration ${String(d.id)} assertionDigest`);need(d.assertionDigest===sha256Json(d.assertionSchema),`declaration ${String(d.id)} assertion digest is invalid`);need(d.lanes?.typespecGeneratedJsonSchema?.role==='comparison-evidence-only',`declaration ${String(d.id)} generated lane role is invalid`);need(d.lanes?.authoredJsonSchema?.role==='independently-authored-authority',`declaration ${String(d.id)} authored lane role is invalid`);}return Object.freeze({admitted:true,irId:contractIr.irId,runId:parityReport.runId,inputs:Object.freeze({...expectedInputs})});}

import assert from 'node:assert/strict';
import test from 'node:test';
import {sha256Json,verifyContractIrAdmission} from './contract-ir-admission.mjs';
const h=(c)=>c.repeat(64);
function f(){const expectedInputs={typespec:h('a'),authoredJsonSchema:h('b'),generatedJsonSchema:h('c')};const parityReport={schema:'ores.typespec-json-schema-validator.report/v1',runId:h('d'),status:'passed',zeroUnexplainedFindings:true,findings:[],inputs:{typespec:{digest:expectedInputs.typespec},authoredJsonSchema:{digest:expectedInputs.authoredJsonSchema},generatedJsonSchema:{digest:expectedInputs.generatedJsonSchema}}};const assertionSchema={type:'string'};const body={schema:'ores.typespec-json-schema-validator.contract-ir/v1',status:'passed',admissible:true,role:'downstream-derived-parity-artifact',editableAuthority:false,authorities:{typespec:'independently-authored',jsonSchema:'independently-authored',generatedJsonSchema:'comparison-evidence-only',precedence:'none'},admission:{receipt:{schema:parityReport.schema,runId:parityReport.runId,digest:sha256Json(parityReport),status:'passed',zeroUnexplainedFindings:true},scope:{complete:true}},provenance:{typespec:{digest:expectedInputs.typespec},authoredJsonSchema:{digest:expectedInputs.authoredJsonSchema},generatedJsonSchema:{digest:expectedInputs.generatedJsonSchema}},declarations:[{id:'Example.Id',assertionSchema,assertionDigest:sha256Json(assertionSchema),lanes:{typespecGeneratedJsonSchema:{role:'comparison-evidence-only'},authoredJsonSchema:{role:'independently-authored-authority'}}}]};return{contractIr:{...body,irId:sha256Json(body)},parityReport,expectedInputs};}
test('admits exact parity evidence',()=>assert.equal(verifyContractIrAdmission({...f(),requireComplete:true}).admitted,true));
test('rejects stale checkout evidence',()=>{const v=f();v.expectedInputs.generatedJsonSchema=h('e');assert.throws(()=>verifyContractIrAdmission(v),/stale for this checkout/);});
test('rejects authority promotion',()=>{const v=f();v.contractIr.authorities.precedence='typespec';assert.throws(()=>verifyContractIrAdmission(v),/no precedence/);});

name: zed_pkg_client
description: Dart SDK for the zed-pkg registry.
version: 0.1.1
repository: https://github.com/zed-pkg/zed-clients

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crypto: ">=3.0.0 <4.0.0"
  http: ">=1.2.0 <2.0.0"

dev_dependencies:
  lints: ">=4.0.0 <6.0.0"
  test: ">=1.24.0 <2.0.0"

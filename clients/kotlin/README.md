# zed-client-kotlin

A Kotlin/JVM client using Java 17 `HttpClient`. Methods return registry JSON as
UTF-8 strings so callers may choose kotlinx.serialization, Jackson, Moshi, or
another JSON library.
